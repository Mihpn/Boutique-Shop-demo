# =============================================================================
# NHÓM 2: Backend Độc lập - shippingservice
# =============================================================================
apiVersion: apps/v1
kind: Deployment
metadata:
  name: shippingservice-online-boutique
  namespace: {{ include "online-boutique.namespace" . }}
  labels:
    app: shippingservice-online-boutique
    group: "2-backend-independent"
spec:
  selector:
    matchLabels:
      app: shippingservice-online-boutique
  template:
    metadata:
      labels:
        app: shippingservice-online-boutique
    spec:
      serviceAccountName: shippingservice-online-boutique
      securityContext:
        fsGroup: 1000
        runAsGroup: 1000
        runAsNonRoot: true
        runAsUser: 1000
      imagePullSecrets:
        {{- range .Values.imagePullSecrets }}
        - name: {{ .name }}
        {{- end }}
      containers:
      - name: server
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop: ["ALL"]
          privileged: false
          readOnlyRootFilesystem: true
        image: "{{ .Values.shippingservice.image.repository }}:{{ .Values.shippingservice.image.tag }}"
        imagePullPolicy: {{ .Values.shippingservice.image.pullPolicy }}
        ports:
        - containerPort: 50051
        env:
        - name: PORT
          value: "50051"
        - name: DISABLE_PROFILER
          value: "1"
        readinessProbe:
          initialDelaySeconds: 20
          periodSeconds: 10
          timeoutSeconds: 5
          grpc:
            port: 50051
        livenessProbe:
          initialDelaySeconds: 20
          periodSeconds: 10
          timeoutSeconds: 5
          grpc:
            port: 50051
        resources:
          requests:
            cpu: {{ .Values.shippingservice.resources.requests.cpu }}
            memory: {{ .Values.shippingservice.resources.requests.memory }}
          limits:
            cpu: {{ .Values.shippingservice.resources.limits.cpu }}
            memory: {{ .Values.shippingservice.resources.limits.memory }}
---
apiVersion: v1
kind: Service
metadata:
  name: shippingservice-online-boutique
  namespace: {{ include "online-boutique.namespace" . }}
  labels:
    app: shippingservice-online-boutique
spec:
  type: ClusterIP
  selector:
    app: shippingservice-online-boutique
  ports:
  - name: grpc
    port: 50051
    targetPort: 50051
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: shippingservice-online-boutique
  namespace: {{ include "online-boutique.namespace" . }}
